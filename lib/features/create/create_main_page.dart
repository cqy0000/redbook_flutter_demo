import 'package:flutter/material.dart';

class CreateMainPage extends StatelessWidget {
  const CreateMainPage({Key key, this.labelId}): super(key: key);

  final String labelId;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('1111'),
      ),
    );
  }
}